# ⚠️ MOVED — use `/UNIT App/native/` instead

This folder is a **stale copy** and must not be used for Xcode builds.

**Active native project:**
```
/Users/directorxklusive/Desktop/UNIT App/native/
```

Open in Xcode:
```
/Users/directorxklusive/Desktop/UNIT App/native/ios/App/unit.xcodeproj
```

See `native/README.md` and `native/CARPLAY.md` in the parent folder.
